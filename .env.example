# Server Configuration
# PORT: The port number the server will run on (also used for frontend API URL)
PORT=5555
# HOST: The host address to bind to (0.0.0.0 = all interfaces)
HOST=0.0.0.0
# DEBUG: Enable debug mode (True/False)
DEBUG=True

# Training Configuration
TRAINING_NUM_EPOCHS=10000
TRAINING_HIDDEN_DIM=64
TRAINING_LEARNING_RATE=0.005
TRAINING_PRINT_EVERY=200
TRAINING_EVAL_EVERY=50
TRAINING_GRAPHS_PER_EPOCH=50

# Graph Generation Configuration
GRAPH_NUM_NODES_MIN=5
GRAPH_NUM_NODES_MAX=12
GRAPH_EDGE_PROB_MIN=0.15
GRAPH_EDGE_PROB_MAX=0.6
